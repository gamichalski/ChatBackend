from django.test import TestCase
from core.payments.models import Payment

# class PaymentTestCase(TestCase):
#     def test_setUp(self):
#         self.assertTrue(Payment.objects.create(user_id=1, amount=100))
        

